import { isArray, isDate, isFunction, isRegExp, isNumber, isObject, isString, isNull, isUndefined } from '../utils';


const parse = (value) => {
    
}

const string2json = (string) => {

}

export default string2json;